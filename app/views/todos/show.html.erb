
<table class="table table-striped table-condensed">
  <tr>
    <td><%= @todo.title %></td>
    <td><%= @todo.date %></td>
    <td><%= @todo.expire %></td>
    <td><%= @todo.note %></td>
  </tr>
</table>

<%=link_to 'Back to index', todos_path %>




